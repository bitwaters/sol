services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - ${SOL_ENV_FILE:-.env}
    environment:
      - TZ=UTC
    volumes:
      - ${SOL_DATA_DIR:-./data}:/app/data
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"

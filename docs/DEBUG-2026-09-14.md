# 个人凭证联调记录（2026-09-14）

范围：GMGN 真实只读采集、信号评估、配置解析、数据库与容器验证。规则版本 `live-2026-09-14.2`。未启用 Telegram 推送；所有实测流水写入独立 `data/debug/` 数据库，未创建或修改正式 `data/meme.sqlite`。

## 已修复

| 实测问题 | 修复 |
|---|---|
| follow 的 `token_amount` 为 0，旧代码优先读它且数量单位未验证 | 优先使用 `base_amount`；通过 USD 交叉计算和同笔公开交易确认可读数量，直接参与计算 |
| 同笔交易在 follow／公开源存在浮点差异，原始金额键会重复累计 | 数量、quote、USD 同时近似且匹配唯一时关联规范事件；保存来源别名；同来源分笔与模糊匹配不强行合并 |
| 后到来源的余额／全平信息丢失；重放也丢弃这些字段 | 合并来源证据后重放，金额只累计一次；缺少可靠成本起点仍保持 unknown，不自动赋予完整成本 |
| 实际只返回 100 条，却按配置 200 判断是否满页 | 两路公开源配置改为 100，poller 兼容旧 200 配置时也按 100 请求和判定 |
| follow 游标未推进，后台回补可能空转并推进水位 | 重复页／游标停止翻页，未覆盖缺口时不推进水位；真正覆盖后才恢复水位 |
| wallet stats 多地址请求只返回第一个钱包 | 网关逐地址请求、每地址独立计费，再聚合结果；两钱包实测均匹配 |
| Docker 缺少 SQLite 原生依赖编译工具 | 仅构建阶段安装 Python、make、g++，运行阶段保持非 root |
| Docker 原先复制整个 data，可能把新生成的私钥带入镜像 | 只复制 CEX 黑名单，并排除 `.env.*`、密钥目录、PEM、调试数据 |

## 验证

- 自动化：15 个测试文件、116 个用例通过；本次新增 13 个契约／去重／重放／分页回归用例。类型检查、生产代码构建、`git diff --check` 通过。
- 实际样本：follow／KOL 重叠的 19 笔成交正确合并；依次导入三路各 100 条样本后得到 269 个事件、300 条来源观测。
- 画像：修复后两个请求地址均返回对应画像。
- Compose：API Key 与 `.env` 一致、私钥 PEM 可解析且公钥一致，`DRY_RUN=1`；仅输出校验布尔值，未输出凭证。
- `.env` 权限 600；`.env`、生成的密钥、原始关注流水均被 Git 忽略。

## 最终版本运行结果

2026-09-14 09:17:43–09:27:43 UTC，同一业务代码版本运行 600 秒，配置版本 `229673f0c68c`。

| 来源 | 轮询次数 | 拉取记录数（含重复观测） | 首次插入事件 | 采集失败 | 429 |
|---|---:|---:|---:|---:|---:|
| smartmoney | 542 | 54,200 | 389 | 0 | 0 |
| KOL | 320 | 31,900 | 138 | 1 | 0 |
| follow | 313 | 31,400 | 217 | 0 | 0 |

- 共 744 个规范事件、786 条来源观测；SQLite `integrity_check=ok`，三来源结束时均无记录中的缺口。
- follow 首次分页出现 1 次重复首页，被检测并停止；之后重叠轮询持续正常。无已检测缺口不等同于证明所有钱包交易完整。
- KOL 一次采集失败后后续轮询恢复；原汇总未保存该次错误类型，原因尚不能确认。调试脚本已补充脱敏错误类型／HTTP 状态／底层错误码统计，供下轮复现定位。
- 候选评估共 54 次：42 次 watching、4 次票数不足、8 次净流入不足；没有产生通过全部条件的推送，本轮不验证真实消息发送。
- 原始汇总：`data/debug/2026-09-14T09-17-43-653Z/summary.json`。排错前两轮短时运行不计入本轮时长。

## 容器验证

- 最终代码在 Linux arm64 上 `docker build --load -t meme-signal-bot:debug .` 成功。
- 使用虚构 Key、`DRY_RUN=1`、`--network none` 的临时容器启动成功，运行 UID=1000；SQLite 读写与重启后的完整性均为 ok。
- SIGTERM 退出码为 0；重启后测试标记保留；Telegram 层未启动；镜像内无 `.env` 和 `data/gmgn-api-keys`。临时容器已删除。
- 隔离容器运行时间较短，未观测到网络请求超时后的退避日志，**不能将此次冒烟认定为完整断网恢复演练**；也未验证 VPS 绑定目录的权限及真实在途推送。

## 待完成门禁与建议

1. **真实分页回补尚未通过。** 当前 `next_page_token` 及另测的 `page_token`／`cursor`／`next` 均未得到更旧数据。应先取得并验证 GMGN 服务端有效分页契约；在此之前保留缺口检测，不能承诺高峰期不漏单。
2. **连续 30 分钟验收未完成。** 本轮是在发现问题后多次重启的调试，不合并不同时段冒充连续验收。使用 `npm run debug:live` 可执行默认 30 分钟只读联调；也可通过 `DEBUG_DURATION_SEC` 指定时长，输出位于独立的 `data/debug/<时间>/summary.json`。
3. **Telegram 与 VPS 验收未完成。** 需要在明确指定的测试群验证消息、编辑、退出提醒与告警送达，再执行 VPS 网络、挂载权限和在途推送恢复演练。现有频道尚未收到本次调试消息。
4. **存量数据升级。** 新别名表自动创建；不会追溯合并历史上已经重复入库的事件，也不会自动换算旧 `base_unit_unverified` 记录。本机正式数据库不存在，当前无待迁移正式流水；其他部署需备份后单独检查历史数据。

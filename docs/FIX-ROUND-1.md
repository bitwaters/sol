# 代码复核修复记录（Round 1）

> 日期：2026-09-13 · 对照：外部复核 22 项问题
> 验证：`npm test` 11 文件 / 49 用例全绿；`tsc` 与 `build` 通过（dist 已含 schema.sql）

## 已修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 构建缺 schema.sql | `build` 增加 `scripts/copy-assets.mjs`，复制到 `dist/store/` |
| 2 | 重复成交重复累计持仓、顺序不保证 | `ingestBatch` 仅在事务内对新插入事件按时间升序回调；回调移入事务 |
| 3 | 回补后不修复持仓 | `backfillFollow` 对迟到事件按 (wallet, token) 调用 `rebuildWalletToken` 重放 |
| 4 | 成本完整性过度推断 | 仅零余额检查点可授予；缺口传播到 `recomputeCostCompleteness` |
| 5 | 均价混入已关闭周期 | 均价只统计 `buy.timestamp >= cycle.cycleStartedAt`（周期∩窗口） |
| 6 | 发送前无完整复核 | 原子领取后检查：状态必须 `sending`、60min 生命周期、TTL、过期即取消 |
| 7 | 评估/发送并发重复 | 评估防抖在完成后释放；推送器重入保护 + 条件更新原子领取 |
| 8 | 已推送信号被错误复用 | 票数跌破 → 降级编辑；`blocked_price` 保持 `pushed` 并降级 |
| 9 | 升级成员未持久化 | 首次推送/升级写入 `signal_wallets`；展示与退出监控优先读取绑定表 |
| 10 | 排队请求穿过封禁门 | 获取预算后、发送前再次检查封禁门 |
| 11 | follow 数量单位未验证 | follow 来源保留原始值、标准数量置空（周期标 incomplete） |
| 12 | 低速率阻塞高权重 | 容量下限 `max(rate, 5)`，覆盖最大权重 |
| 13 | 打满反而降频 | 打满时间隔减半（下限 250ms），测试同步修正 |
| 14 | 无定时重验/过期调度 | 每 60s 维护：过期清理 + 对活跃候选触发重验；不活跃按"最后一笔合格买入"判定 |
| 15 | 非共识退出误报 | 仅统计推送后发生（`last_sell_ts >= sent_at`）且非绑定成员的钱包，按钱包去重 |
| 16 | 归档边界与重写 | 计数/导出/删除统一 `timestamp < cutoff`；重写前删除旧文件 |
| 19 | 对照组口径不一致 | 复用 `buildClusters` 聚类计票 |
| 20 | 统计分组漂移 | 使用触发时快照的年龄/市值，不读当前缓存 |
| 21 | DRY_RUN 下回测/采样/备份未启动 | 定时器移出推送开关，仅消息发送依赖 Telegram |
| 22 | 编辑节流未实现 | `shouldEscalate` 内容变化检查 + `edit_last` 节流；超停止编辑窗口改发跟进消息 |

## 未完成（遗留）

| # | 问题 | 状态 |
|---|---|---|
| 17 | 回测队列头阻塞 | ✅ 已修复：按"最早到期缺失期限"排序，同一期限缺行情 3 次后放弃 |
| 18 | 被拦截候选未回测 | ✅ 已修复：纳入 `invalidated` / `blocked_price`，按拦截原因分组报告 |
| 14b | 硬过滤禁验期 / 重新触发冷却 | ✅ 已修复：硬过滤失败 10 分钟禁验；候选结束后 5 分钟重新触发冷却 |
| — | 回归测试 | ✅ 已补 4 个：重复采集持仓幂等、并发发送仅一次、TTL 取消、归档重写不追加 |

**验证**：`npm test` 12 文件 / 53 用例全绿；`tsc` 与 `build` 通过。

**结论：复核问题全部处理完毕。正式群推送仍需按 `docs/GO-LIVE-CHECKLIST.md` 完成真实凭证与环境验收后启用。**

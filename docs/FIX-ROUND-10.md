# 代码复核修复记录（Round 10）

> 日期：2026-09-13 · 对照：复核 3 项（基线 `6eb8da1`）
> 验证：`npm test` 12 文件 / 54 用例全绿；`tsc` 与 `build` 通过

## 已修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 已取消任务未真正恢复 | 冲突更新对可恢复任务**原子置回 `pending`**：`cancelled` 恢复时重置 `attempts=0`、`next_retry_at=NULL`，并保留其他状态的重试进度 |
| 2 | 重建误绑历史信号到最新周期 | 仅校正**受影响范围**（`cycle_no >= minCycle`）的绑定，保留正确的历史绑定；覆盖 `pushed` / `sending` / `candidate` 三类信号 |
| 3 | 静默判断使用复核前时间 | 复核完成后**重新读取当前时间**，用于静默规则判断与任务延后写回 |

**结论：本轮 3 项全部处理。正式群推送仍需按 `docs/GO-LIVE-CHECKLIST.md` 完成真实凭证与环境验收。**

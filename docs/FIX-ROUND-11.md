# 代码复核修复记录（Round 11）

> 日期：2026-09-13 · 对照：复核 2 项 P1（基线 `b68968c`）
> 验证：`npm test` 12 文件 / 54 用例全绿；`tsc` 与 `build` 通过

## 已修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 恢复任务绕过重试等待与次数上限 | 冲突更新仅在原状态为 `cancelled` 时置 `pending`（并重置重试字段）；`failed` / `unknown` **保留原状态、attempts 与 next_retry_at**，重试调度仍由推送器控制 |
| 2 | 重建误绑历史信号到最新周期 | 改用**周期起点映射**：重建前记录旧周期 `cycle_started_at`，重建后按"起点不晚于旧起点"的最近周期映射；仅更新实际变化的绑定，历史绑定保持不动 |

**结论：本轮 2 项全部处理。正式群推送仍需按 `docs/GO-LIVE-CHECKLIST.md` 完成真实凭证与环境验收。**

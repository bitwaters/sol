# 代码复核修复记录（Round 3）

> 日期：2026-09-13 · 对照：复核 9 项（基线 `a1bdfc1`）
> 验证：`npm test` 12 文件 / 53 用例全绿；`tsc` 与 `build` 通过

## 已修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 旧归档文件丢数据 | 归档前**合并已有文件**（按 `event_id` 去重），临时写入并校验总数后才删除 DB 记录；兼容旧版部分日归档 |
| 2 | 升级重写成员表漏退出提醒 | 改为**增量写入**：保留原成员加入版本与周期绑定；已有成员仅原子协调簇映射，新增成员追加 |
| 3 | 缺口超时后恢复完整性 | 缺口撤销时持久化 `gap_affected:{token}`；超时解除阻塞也不恢复，**仅可靠重建清除** |
| 4 | 富化期间持仓变化未复核 | 富化完成后、创建任务前**再次执行钱包层校验**，不通过则中止 |
| 5 | TTL 取消后候选卡 sending | 取消时置 `expired`（非复用状态）并写入重新触发冷却；新候选走新行/新任务 |
| 6 | 重评清零重试次数与退避 | 任务 upsert 仅更新 payload，**保留状态、attempts、next_retry_at**；重试调度归推送器 |
| 7 | 放弃样本仍堵回测队列 | 在 SQL `LIMIT` 前用 kv 中 `backtest_giveup:*` 排除终止样本 |
| 8 | 票数分组随更新漂移 | 分组读取快照 `votes`；缺失快照标记未知，不用当前值替代 |
| 9 | 退出监控未传 CEX 名单 | `runExitMonitor` 传入已加载的 `blacklist`，与信号评估共用聚类依赖 |

## 验证

- 回归测试更新：TTL 取消后信号为 `expired`
- 归档合并逻辑使重复运行保持行数不增、旧数据不丢

**结论：本轮 9 项全部处理。正式群推送仍需按 `docs/GO-LIVE-CHECKLIST.md` 完成真实凭证与环境验收。**

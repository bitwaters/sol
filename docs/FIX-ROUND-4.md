# 代码复核修复记录（Round 4）

> 日期：2026-09-13 · 对照：复核 6 项（基线 `3d22ab1`）
> 验证：`npm test` 12 文件 / 53 用例全绿；`tsc` 与 `build` 通过

## 已修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 归档非原子写入 | 写入同目录 `.tmp`，校验通过后 `renameSync` 原子替换；任何失败保留旧文件；旧归档读取失败则跳过该日清理 |
| 2 | 发送复核路径错误 | `revalidateSignalForSend` 在异步富化后**重新读取窗口/聚类/钱包层**再判定，与 `evaluateToken` 的发送前复核一致 |
| 3 | 历史与新成员簇编号碰撞 | 簇 ID 在信号范围内**稳定分配**：优先复用历史成员已有 ID，否则从未占用编号中分配；历史成员不重写 |
| 4 | 重建解除整币缺口保护 | `gap_affected` 改为**按钱包+代币**持久化；重建只清除该钱包标记，其余保持不可核验 |
| 5 | 放弃单期限终止整信号 | 移除 SQL 层按信号排除；保留**按信号+期限**的逐项过滤与终止 |
| 6 | 缺基准价格样本堵队 | 在占用本轮额度前检查基准价；缺失则**逐期限标记终止**并跳过，继续填充有效任务 |

## 验证

- 归档：临时文件 + 原子替换，重复运行行数不增、旧数据不丢
- 测试保持 53 用例全绿（含归档、回测、并发、TTL 回归）

**结论：本轮 6 项全部处理。正式群推送仍需按 `docs/GO-LIVE-CHECKLIST.md` 完成真实凭证与环境验收。**

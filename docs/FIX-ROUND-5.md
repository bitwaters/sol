# 代码复核修复记录（Round 5）

> 日期：2026-09-13 · 对照：复核 3 项（基线 `4c20e44`）
> 验证：`npm test` 12 文件 / 53 用例全绿；`tsc` 与 `build` 通过

## 已修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 新买入绕过缺口标记恢复完整性 | `deriveCostComplete`（所有授予入口）统一检查 `gap_affected:{token}` 与 `gap_affected:{token}:{wallet}`；普通成交不再恢复资格，仅可靠重建解除 |
| 2 | 富化期间新增缺口仍可推送 | `revalidateSignalForSend` 在异步富化后：重新读取候选状态、**用最新时间重新检查来源缺口**，并以该缺口结果传入钱包校验；通过后才返回 ok |
| 3 | 终止回测记录占满扫描窗口 | 改为**分页扫描直到填满本轮额度**（pageSize = maxPerRun×20），逐样本按"信号+期限"过滤终止项；额度满或扫描尽时停止 |

## 验证

- 成本完整性授予入口已统一（成交路径与重算路径同源）
- 回测分页扫描保证后续有效样本不被旧终止记录阻塞

**结论：本轮 3 项全部处理。正式群推送仍需按 `docs/GO-LIVE-CHECKLIST.md` 完成真实凭证与环境验收。**

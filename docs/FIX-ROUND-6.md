# 代码复核修复记录（Round 6）

> 日期：2026-09-13 · 对照：复核 1 项（基线 `f314a53`）
> 验证：`npm test` 12 文件 / 54 用例全绿；`tsc` 与 `build` 通过

## 已修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 发送前复核使用异步等待前的时间 | `revalidateSignalForSend` 在异步富化完成后**重新获取时间** `nowAfter`，统一用于：候选状态与有效期判断（>60min → expired）、来源缺口检查、窗口重算、钱包层校验；不再复用入口 `now` |

## 回归测试

新增 `等待期间跨出窗口`：3 笔合格买入距窗口过期 1 秒，富化期间时钟推进 2 秒 → 发送前复核返回 `send_recheck_votes`，拒绝发送。

**结论：本轮 1 项处理完毕。正式群推送仍需按 `docs/GO-LIVE-CHECKLIST.md` 完成真实凭证与环境验收。**

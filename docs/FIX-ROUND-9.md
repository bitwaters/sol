# 代码复核修复记录（Round 9）

> 日期：2026-09-13 · 对照：复核 5 项（基线 `b3e1adf`）
> 验证：`npm test` 12 文件 / 54 用例全绿；`tsc` 与 `build` 通过

## 已修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 富化异常覆盖已推送状态 | 富化失败分支改用 `updateSignalIfReusable`，带状态条件，保护 `pushed` |
| 2 | 取消任务无法随候选恢复 | 任务 upsert 的冲突更新条件加入 `cancelled`：候选重新合格时原子恢复待发送任务 |
| 3 | 重建未校正信号绑定 | 重建事务内按成员加入绑定与重建后开放周期建立映射，更新 `signal_wallets.cycle_no`（仅 `pushed` 信号），避免误报清仓 |
| 4 | 连续重建复用过期检查点 | 重建时**删除检查点之后的旧累计快照**（无检查点则清空该钱包/代币全部检查点），防止二次重建丢失已补成交 |
| 5 | 静默时段发送复核后弱信号 | 完整复核后按**最新有效票数**再次判断静默规则；不满足则任务恢复 `pending` 延后（不消耗预算） |

## 验证

- 重建：检查点失效 + 绑定校正同一事务内完成
- 推送：静默规则在复核后判定，与消息票数口径一致

**结论：本轮 5 项全部处理。正式群推送仍需按 `docs/GO-LIVE-CHECKLIST.md` 完成真实凭证与环境验收。**

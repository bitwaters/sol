# SEA 正式推送部署（2026-09-14）

用户明确授权：继续确认 GMGN 分页；只保留 GMGN 跳转；将本地 Telegram 配置上传 SEA 并启用正式推送。

## 变更

- 正文交易链接及按钮只保留 GMGN，键盘保留刷新与屏蔽；配置校验拒绝其他交易平台。
- `scripts/prepare-sea-env.mjs --live` 只导出 GMGN、Telegram 四项及运行必需字段，文件权限 600；默认命令仍输出 DRY_RUN=1。
- 本地 `.env` 的 DRY_RUN 保持原值；SEA 部署文件设为 0，GMGN 预算仍为每秒 10、容量 5。
- 正式发送仍需满足策略阈值、有效期、完整性和发送前复核，不通过人为降低阈值制造推送。
- 分页诊断新增显式 `--variants`：检查最后一条记录 ID 作为 cursor、next_page_token、page_token，间隔 3 秒，任一请求失败即停止。只输出聚合数量。

## 验证与部署

本地类型检查、18 个测试文件的 132 项测试、构建通过。代码先推送 GitHub，再由 SEA 快进拉取和构建。实际部署证据见下，不将历史一次性测试冒充持续推送验收。

## 尚待人工或供应方验证

GMGN 分页在官方文档中只有返回字段描述，官方 CLI 未暴露分页输入。本次诊断仍无法推进，继续保留缺口保护并记录限制。线上管理员命令/按钮需由真实管理员操作验证；自动化权限测试已通过。真实在途 Telegram 请求的破坏性重启测试尚未做。

## 实际部署证据

- 应用提交 `b01055f`；运行配置指纹 `0d51662c1191`，规则版本仍为 `live-2026-09-14.5`（本次不改策略）。
- 2026-09-14 11:23:26 UTC 启动正式服务；Telegram 启动日志成功，信号与告警目标的 Bot 身份均为管理员。
- SEA 已加载 Telegram 四项配置，管理员 1 位；`DRY_RUN=0`、`paused=false`、`chain=sol`、`links=[gmgn]`。
- `/etc/sol/sol.env` 模式 600，数据目录 700；本地 `.env` 保持 DRY_RUN=1，凭证不进入 GitHub。
- 首次上线检查：数据库 quick_check=ok，三路心跳 0–1 秒，无缺口，错误和 429 均为 0，无自动重启。当前未出现满足完整策略的真实信号，推送任务为空；不将 Bot 已启动等同于已有真实信号送达。
- 保留上线前镜像 `sol-bot:rollback-before-live`。应用配置由 Compose 解析；一次性诊断采用 root 身份只读挂载 600 权限的 dotenv 文件，避免 `docker run --env-file` 将引号作为字面值传入。前两次诊断在配置校验阶段退出，未请求 GMGN。

## 分页确认结果

本次最多 5 次真实只读请求，采集进程短暂停止以避免配额竞争，随后恢复。首页 100 条；顶层 token 作为 next_page_token 时重叠 100 条、无更旧记录；最后记录 ID 分别作为 cursor、next_page_token、page_token 时均重叠 98 条、无更旧记录（首页新增了 2 条）。四种请求均成功但没有历史推进，未触发 429。

因此已确认这些参数组合在当前账号上不能有效分页；无法仅据此断言服务端完全不支持分页或账号套餐受限。供应方协议仍需确认，不对完整历史覆盖作承诺，详见 `GMGN-PAGINATION-ISSUE.md`。

上线后复查（11:25:55 UTC）：采样时运行 132 秒，三路心跳 1–2 秒、无缺口，数据库 5,210 条事件（首次采样 5,123 条），quick_check=ok；完整启动后日志错误 0、429 为 0、Telegram 启动成功 1 次、失败 0 次，自动重启 0 次。推送任务仍为空，没有把虚构数据注入生产队列。

# SEA 正式推送部署（2026-09-14）

用户明确授权：继续确认 GMGN 分页；只保留 GMGN 跳转；将本地 Telegram 配置上传 SEA 并启用正式推送。

## 变更

- 正文交易链接及按钮只保留 GMGN，键盘保留刷新与屏蔽；配置校验拒绝其他交易平台。
- `scripts/prepare-sea-env.mjs --live` 只导出 GMGN、Telegram 四项及运行必需字段，文件权限 600；默认命令仍输出 DRY_RUN=1。
- 本地 `.env` 的 DRY_RUN 保持原值；SEA 部署文件设为 0，GMGN 预算仍为每秒 10、容量 5。
- 正式发送仍需满足策略阈值、有效期、完整性和发送前复核，不通过人为降低阈值制造推送。
- 分页诊断新增显式 `--variants`：检查最后一条记录 ID 作为 cursor、next_page_token、page_token，间隔 3 秒，任一请求失败即停止。只输出聚合数量。

## 验证与部署

本地类型检查、18 个测试文件的 132 项测试、构建通过。代码先推送 GitHub，再由 SEA 快进拉取和构建。后续记录补充实际部署证据，不将历史一次性测试冒充持续推送验收。

## 尚待人工或供应方验证

GMGN 分页在官方文档中只有返回字段描述，官方 CLI 未暴露分页输入。若本次诊断仍无法推进，保留缺口保护并记录限制。线上管理员命令/按钮需由真实管理员操作验证；自动化权限测试已通过。真实在途 Telegram 请求的破坏性重启测试尚未做。

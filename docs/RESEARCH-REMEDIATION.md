# 宽范围研究、规则比较与离线回放

本版协议 `research-2026-09-14.1`。用户要求开发阶段按样本数量准入，不设置 14 天或任何最短运行天数。期限行情仍需等到实际到期，不能把尚未发生的 1 小时/24 小时结果当作有效结果。

## 交付范围与数据口径

| 任务 | 实现 | 验收依据 |
|---|---|---|
| T1 研究配置独立 | `research.json`、`research/config.ts`；配置哈希和测量协议同时记录 | 研究配置与生产配置分开；生产规则版本不变 |
| T2 全部规则诊断 | `research/diagnostics.ts` | 原始值/上下限/通过/失败/未知/不适用；钱包可同时命中多个原因；后续汇总不因前一条失败而停止 |
| T3 冻结状态 | `research/snapshot.ts`，压缩保存在研究表 | 原始入选与完成时点快照分别保留；包含事件及来源到达时间、画像、持仓、检查点、当时观察次数、缺口、黑名单及配置 |
| T4 扩大采样 | `research/collector.ts` | 最近120秒有正金额买入的已观测SOL代币，按正式判断窗口内达到金额门槛的买入地址数0/1/2/3/4/5+分层；不沿用300美元、3票、市值等筛选条件 |
| T5 有界富化 | 共用 `gateway.background()` | 前台预算、封禁、排队机制不变；研究不写共享画像/代币缓存，记录排队和富化失败 |
| T6 独立实验 | `research/replay.ts`、`research/report.ts` | 同一快照原规则与单因素方案对比；原规则失败但新规则通过的对象可进入“新增组” |
| T7 报告 | `/stats`、每日统计及两个CLI | 研究与旧版分开，数量/覆盖率/逐条拦截/缺资料原因；分层入选概率可审计 |
| T8 事件和状态回放 | 内存SQLite重建，调用正式窗口/聚类/钱包/代币校验器 | 窗口最大60分钟；金额、动作、画像门槛、关联参数可重算；更改来源/尘埃阈值时须具备可靠起点 |
| T9 投递回放 | 执行前有界冻结，内存运行真实候选/退出/Pusher实现及假发送器 | 支持静默、限速、TTL、编辑、冷却、退出门槛；可指定成功/429/结果未知情景，不产生真实消息 |
| T10 路径和成本 | `research/outcomes.ts`、`research/cost.ts` | 已完成K线端点、收盘路径完整率、收盘最大回撤、明确输入的成本情景 |

范围是**已采集到的SOL交易及被抽取的观察时点**，不是全市场、全部历史时点或连续策略回测。只有快照和事件证据足够的样本才可回放；其余明确标记不可验证，不用当前资料补造历史。

## 采样、预算和存储

- 默认每300秒检查一轮，6个地址数层各最多1个代币，共最多6个；重启保留采样时间。地址数分层只决定抽样，分析另算聚类前后和过滤前后票数。
- 同层按版本、采样时间和代币的稳定哈希排序，入选概率为该层实际入选数/候选数。队列容量不足时轮换层的优先顺序；报告保留各层总体数与入选数。这里的权重只描述实际抽样总体，不代表全市场。
- 待处理最多24个；新样本每30秒独立调度，单批最多2个，不等待旧回测批次。到期研究结果单批最多8个（独立于采样富化批量），旧体系补价和结果各最多1个样本；有待处理研究样本时暂缓启动下一项慢速回测任务；新样本每次最多补2个缺失/过期钱包画像。共用每秒最多1权重的后台预算及原全局预算，繁忙即让路。容量5时，代币/K线请求预留3权重；画像请求本身权重3，预留2权重，避免要求6权重而永久无法执行。总速率与容量不增加，已有前台队列仍优先。
- 新样本允许120秒内完成富化。此时重新冻结整个窗口、状态及基准时间，原始入选快照保留。超过期限沿用原始快照，不使用后来的报价或画像。
- 基准价缺失的研究样本保留在分母，本版不把历史补价标成实时研究样本。原测量体系的历史补价功能继续运行。
- 单样本最多5000条相关钱包的保留成交、100个相关钱包；超过上限记录 `snapshot_size_limit`，不静默截断成“完整样本”。压缩快照独立存储在运行数据库中，不受成交30天清理影响。
- 投递快照保留同币历史投递及成员绑定，避免回放遗漏去重状态。仅在有待执行任务时记录，同任务状态每分钟最多一份；最多200个任务、4个代币、1000个相关信号，压缩后最多2MB。超过限制保留失败计数。此范围不承诺捕获所有可能状态。
- 研究数据随数据库一致性备份，原始响应、画像、地址、聊天标识及快照不得进入GitHub或公开报告。CLI输出仅汇总。

## 判定和比较

“输入完整”不要求所有策略通过。缺少成本依据导致某个派生指标不可计算时，该项标为未知；若实验改变条件后具有成本依据，则可重新判断，不能直接用零代替。

原配置每条校验独立记录。后续发现与样本窗口重叠的历史缺口也会撤销比较资格，冻结的策略输入仍保持原样。为了计算后续持仓、聪明钱等汇总，研究使用不改变钱包剔除条件的分析副本，去掉前置汇总门槛；生产校验器结果也同时记录，供一致性检查。

实验改变一个因素；有效票数联动两个下限。结果分成：原规则与实验均通过、仅实验通过（新增）、仅原规则通过（移除）、均不通过。比较报告将前者作为保留组，新增/移除作为变化组。全部不通过的对象仍在诊断报告中，但不当作被修改规则单独造成的差异。

- 默认仅比较最新生产配置、规则及研究版本；同一版本内，同一代币先按**首次入选**去重，再判断数据质量，首次失败不会被后续成功替换。旧版本样本不删除、不升级成新版本；不同采样版本的结果不合并为独立样本。
- 按生产配置、规则、研究版本和来源组合分别比较。报告给出普通中位数及按抽样概率加权的中位数，两者都只适用于已抽样总体。
- 未注册实验可以立即查看探索性比较，按时间排序后的样本数量70%/30%分组，没有日历天数门槛。
- 正式留出验证先注册参数和值，固定当前最大样本ID作为边界。注册后在该固定版本内首次入选的代币才是留出样本；不允许更改注册定义后复用原ID。
- 准入：至少20个有效独立代币；训练/留出各自的保留组、变化组，每格至少20个有效独立代币；每格到期1小时覆盖率至少90%；整体及该版本来源组实时基准覆盖率至少95%。没有固定天数限制。
- 达标仅意味着可检查描述性差异，不自动修改生产配置，也不证明收益优势。反复试验、分组太细或几乎没有差异都可能导致结论不足。

旧版 `compareSamples` 也移除了14天门槛，改为按时间排序后的数量70%/30%分组；历史/旧版样本继续保持原资格，不能混入新研究组。

## 离线命令

在已部署容器内执行；路径为运行数据库。`overview`、`compare`、回放和成本命令只读；`register` 仅写实验定义，不修改生产配置。

```sh
node dist/ops/research-report.js /app/data/meme.sqlite overview
node dist/ops/research-report.js /app/data/meme.sqlite explore
node dist/ops/research-report.js /app/data/meme.sqlite compare validVotes 2
node dist/ops/research-report.js /app/data/meme.sqlite compare netInflow 1000
node dist/ops/research-report.js /app/data/meme.sqlite compare windowMinutes 30
node dist/ops/research-report.js /app/data/meme.sqlite register validVotes 2
# 将返回的ID代入：
node dist/ops/research-report.js /app/data/meme.sqlite compare validVotes 2 EXPERIMENT_ID
node dist/ops/research-replay.js /app/data/meme.sqlite delivery '{"parameter":"maxPerMinute","value":10}' success 0
node dist/ops/research-replay.js /app/data/meme.sqlite delivery '{"parameter":"ttl","value":90}' 429 100
node dist/ops/research-replay.js /app/data/meme.sqlite cost '{"notionalUsd":100,"buySlippage":0.01,"sellSlippage":0.01,"buyFee":0.005,"sellFee":0.005,"networkUsd":0.01}'
```

支持的研究轴以 `research/replay.ts` 的白名单为准；配置结构及联动约束仍须合法。上述数值仅展示调用方法，不是已验证推荐参数。

## 回放与价格限制

- 窗口、金额、聚类等回放在同一已冻结观察时点重新计算，不据此宣称还原了改规则后“首次会在何时触发”。
- 尘埃阈值重放须有早于已保存成交的可靠零余额检查点。来源过滤重放须从已观测、无缺口的代币创建起点开始，且起点在冻结覆盖范围内；无法确认时拒绝重算，避免混入其他来源的余额证据。
- 投递回放使用真实执行器与假发送器，所有写入只发生在内存库。需要未记录的新行情/画像时禁止联网并标记不可验证；指定时间推进也不会生成该期间的新成交。成功/429/结果未知是人为指定的传输情景，不是对Telegram实际结果的预测。
- 研究端点5分钟/1小时使用1分钟K线，24小时使用5分钟K线；收盘不得晚于目标时间，偏差最多一个周期。零终点价格有效；缺行情每隔至少5分钟重试，三次耗尽；网络错误和后台繁忙不消耗缺行情次数。
- 路径单独核对蜡烛连续性，缺路径不伪装完整回撤。最大涨跌和回撤基于收盘，不能代表蜡烛内极值。
- 成本报告显式指定交易金额、双边滑点、手续费和网络费，使用固定期限退出的假设，不含成交容量、实际成交及可卖性验证；不是实盘收益率。

## 发布验收

本地测试与review修复 → GitHub提交推送 → SEA检查无意外修改并快进部署 → 只读检查研究采样、正式采集、推送状态、日志和数据库完整性。保持用户此前授权的正式Telegram运行模式，不增加研究信号推送。


## 2026-09-15 活跃样本与补采整改

生产策略参数保持原值，研究版本升级为 `research-2026-09-15.3`。

- 只从最近120秒仍有正金额买入的代币中选样，分层计数使用正式窗口（当前15分钟）内单笔达到金额门槛的买入地址数；0层保留低于300美元的活跃对照，1/2/3/4/5+层分别保证不同合格地址数的抽样机会，仍允许低于3票的对象入选。快照仍保留原有最多60分钟回放范围及有界完整保留历史，不把只截取近期历史冒充完整证据。此版本描述的是活跃代币总体，不代表休眠币或全市场。
- 画像补充优先处理正式窗口内达到金额门槛、最近买入的钱包；保持次数上限和隔离缓存。超过5000成交/100钱包的快照仍明确失败，不能通过静默截断提高覆盖率。
- 研究行情批量由2提高到8；半批优先槽位用于当前研究/配置/规则版本的全部到期任务（包括重试），剩余槽位按全局到期时间补齐并去重，持续处理旧任务。每次请求前检查新样本队列，有采集任务即让路。共用原后台/全局预算，不提高接口限速。
- K线查询范围尾部多取两个周期，但有效收盘仍不得晚于目标，偏差仍最多一个周期。严格区分空响应、行情过旧、只有未来K线、结构/字段异常、请求失败、后台让路。保存检查时间、有效K线数量和最新收盘时间；不记录请求凭证或公开钱包明细。旧版耗尽记录保留为“旧版缺失原因未分类”，不凭空归因。
- `explore` 分项比较：票数2/3；净流入500/1000/2000美元；币龄上限360/720/1440分钟；是否必须包含聪明钱。分别给出单项通过、完整通过、新增组及训练样本数量，不把多条同时不满足的对象归因给单个规则。
- 预定研究方案为2票、1000美元、720分钟、不强制聪明钱，四项分别实验。自动检查最多每300秒一次，仅在原规则保留组和变化组各有20个有效独立训练代币、每组1小时到期覆盖率90%、总体及来源组基线覆盖率95%，且总有效独立数达到配置要求后登记。不会根据收益高低挑选登记时点。登记只写实验定义和版本/最大样本ID边界，不更改正式配置或发送研究信号。
- 登记后才开始累计留出样本。相同版本、相同定义重复登记返回原ID和边界，不能重置边界把留出数据挪入训练。完整回放仍是正式比较依据，探索性阈值统计只用于诊断和数量准入。没有固定天数门槛，也不保证市场会产生足够的保留/变化样本。
- 正式候选拦截新增“金额合格地址数不足”和“过滤前聚类票数不足”分类；原“有效票数不足”留作钱包过滤后的原因。旧记录不改写，两类旧原因不能事后强拆。`/stats` 增加当前版本首次样本数量、待补行情和中文缺失原因。

抽查两个历史耗尽样本，即使扩大查询范围，目标附近仍无合格已收盘K线；不能把未来价格或很早的价格当成目标收益。上述改动改善选样和处理能力，不承诺历史缺失能被补齐。


第二次运行修复：原始正金额地址分层仍可能每层都没有300美元合格买入，因此改为0/1/2/3/4/5+金额合格地址层；各层继续使用稳定哈希和真实入选概率，不按未来收益挑样。富化同一轮优先处理高合格地址层，层内顺序不变。后台繁忙重试时，在有界、进程内的样本缓存中保留已完成画像及token info/security请求；响应保留原请求时间，超过60秒重新请求，重启则安全丢弃进度。原始冻结快照不改写，重试也不能给旧价格重新标注为当前价格。


快照进一步缩小到最大回放窗口（60分钟）内出现过任何买卖的相关钱包，并保存这些钱包全部保留历史、来源、画像、周期及检查点。净流入窗口内全部交易、活跃钱包间历史同交易聚类及持仓重建证据仍完整。窗口外从未参与本次判断的钱包不再占用快照额度；超过5000条/100钱包的相关历史仍拒绝，不截断。历史失败样本不反向改成实时成功样本，新范围使用独立研究版本。实测一例从1196条/141钱包缩为107条/11钱包；另一例仍有13925条/199钱包，继续标记不可验证。

后续缺口与归因修复保持 `research-2026-09-15.3`、抽样方式和正式策略参数不变：采集确认水位与最近页头分开，连续重叠页不再扩大旧缺口；缺口更新批量撤销成本资格，避免每轮逐钱包重复写入造成采集延迟。既有历史缺口和冻结样本不缩短、不重写。

新诊断将钱包原因分为行为与数据两类：已清仓、快进快出属于行为；缺少持仓、起点未知、历史不完整、历史缺口影响成本及缺少可靠成本起点分别记录。已清仓不再额外计作成本缺失。快照保存相关钱包当时的缺口标记。`explore` 以当前范围首次独立代币样本中的钱包观测为单位汇总行为独有、数据独有、两者都有、无上述原因及旧版未分类；同一钱包跨代币可重复计数，原因可重叠。旧诊断缺少拆分字段时明确归入旧版，不反推归因。各单因素方案还列出单项通过但完整规则未通过的剩余阻拦项和计数，不能把这些对象当成调参后新增合格样本。

## 2026-09-21 停采与补采失活修复

- 启用来源超过60秒没有成功响应即阻止新信号资格确认；即使该来源已不出现在当前代币窗口中，也不能绕过门禁。持续失活不适用普通成交缺口的10分钟接受策略。
- `source_outages` 独立保存最后成功响应至恢复的墙钟区间。启动时先记录现存旧心跳，再开始采集；监测期间延长区间，首个成功响应原子关闭区间。定时检查尚未运行时，首个恢复响应也会补记停采区间。空页成功属于正常心跳，成交量少不等于失活。
- 停采开始及恢复时批量撤销受影响持仓的成本资格，期间新成交携带持续缺口标记；恢复不授予旧成本资格。快照保存停采表、启用来源和服务起点。比较同时核对实时数据库中的停采历史，恢复后也不会把受影响的旧窗口重新当成完整数据。
- 多来源同时失活时合并执行一次批量撤销；其他来源仍失活时维持全局门禁，最后一路恢复时统一推进成本缺口末端。已为真的标记和未增大的末端不重复写入，避免大库启动时反复更新相同持仓。
- 为活跃代币抽样增加按链、方向、时间的覆盖索引，为已观测买入次数增加钱包、方向、时间覆盖索引。抽样与计数不再依赖扫描含大体积原始响应的历史成交表；升级时首次创建索引可能延长启动，之后仍采用原抽样定义与金额口径。
- 研究版本变为 `research-2026-09-21.1`，以区分新增的来源完整性契约；旧样本、原始价格和失败原因不改写、不删除，旧版行情任务继续按原规则补采。新版本重新按首次代币累计，门槛仍由样本数及覆盖率决定，正式策略阈值不改。
- 每次网络请求都受独立截止时间约束；超时走请求失败重试，不占用“缺行情三次”次数。底层调用挂起时也会释放后台预算及研究任务锁，迟到结果被丢弃。取价仍只能使用目标时点前、允许偏差内的收盘价。
- 单因素探索新增“剩余阻拦组合”及出现次数，展示同一个对象同时失败的规则。组合仅作诊断，不自动产生多参数修改或放宽数据完整性门槛。

## 2026-09-21 研究报告与通知契约

正式信号统计与研究摘要分开；管理信息仅在管理员私聊中展示。`/research` 使用当前运行配置/规则/研究版本，即使当前版本没有样本也不会回退旧版本。首次独立代币、基准价完整、无缺口可比较样本和已到期结果分别展示；详细拦截与历史版本按需打开。投递快照增加成功发布状态和退出事件去重状态。原消息编辑实验不再支持；旧 `stopEditAfter` 轴明确返回不支持，而不是伪装生效。规则版本升级为 `live-2026-09-21.1`，历史样本保留，分版本统计。

退出提醒改为单条消息维护；投递快照加入 `exit_message` 定位及最后成功状态，离线重放能够区分首次发送与后续编辑。该调整不改变信号筛选阈值与研究样本采集口径。

# SEA 持续联调（2026-09-14）

## 范围

保持 DRY_RUN；所有代码通过本地修改、GitHub 推送、SEA 快进部署。真实 Telegram 发送和 Telegram 凭证传输需要用户明确授权，测试脚本不会启动正式推送器。

## 本轮修复

1. 历史缺口按现有策略到期接受后，水位只推进到较旧的重叠记录。该记录滚出最新 100 条后，同一缺口反复开启。现在推进到该来源已经观测到的最新时间，并保存接受区间、次数及 `recovered=false`。受影响持仓的成本资格继续撤销；没有补齐历史交易，也没有清空数据库。
2. 评估执行期间的新成交曾被简单去重丢弃。现在标记代币有更新，当前评估结束后合并为一次重验，同代币仍不并发执行。
3. GMGN 客户端原先只读取限流响应头，忽略响应体 `reset_at`。现读取两者并采用更晚的截止，缺失时才使用网关保守等待策略。
4. Telegram 传输失败缺少确定响应时，现在进入 `unknown`，保留次数和重试时间；确定的 API 错误仍为 `failed`。不保证 Telegram 在不确定重试场景下绝对不重复送达。
5. Trojan 链接改为官方 `solana_trojanbot`，移除没有验证依据的代币 `start` 参数；按钮表示打开交易页面/机器人，不表示应用执行交易。来源：[官方深链说明](https://docs.trojanonsolana.com/about-trojan-on-solana/deep-link-setup)。Photon/BullX 登录后的代币定位仍需界面验收。

## 耗时观测

新增 GMGN 额度排队、请求成功/错误/限流耗时，采集整轮、本地入库、新事件首次观测年龄，代币评估排队/执行/资料补查，以及推送排队/复核/发送/编辑耗时。

- 每 60 秒写一次数值汇总到运行数据库和日志；不记录钱包或凭证标签。
- count/mean/max 自本次进程启动累计；P50/P95 取各项最近最多 2,048 个样本，`sampleCount` 明确标示样本量。
- `poll.event_age` 包含 GMGN 收录及轮询延迟，启动历史页会拉高数值；不等同于纯网络请求耗时。
- `evaluation.result.*` 的 count 是评估结果次数，时间值为 0，不是延迟指标。
- 250ms 是队列启动延迟，最多两个评估并发，不是强制的逐任务间隔。
- DRY_RUN 不产生 Telegram 发送样本；缺少样本时不报告推测的真实送达延迟。

## 验证工具

- `node dist/ops/status.js`：只读运行快照，包含数值耗时和历史缺口接受记录。
- `docker logs --since <UTC时间> sol-bot-1 2>&1 | node scripts/analyze-runtime.mjs`：仅输出白名单汇总，不展示原始成交。
- `node dist/ops/probe-follow.js --run`：最多两次真实只读请求，间隔 3 秒，输出第二页重叠/更旧记录数。429 后停止，不猜测无限分页参数。
- `node dist/ops/fault-drill.js`：本机模拟 HTTP API、断开连接、真实 HTTP 429、封禁后恢复及 SQLite 重开。可在 `--network none` 的隔离容器中执行；不使用真实密钥、不触碰生产数据。
- `node dist/ops/telegram-smoke.js --preview`：预览虚构信号、升级编辑、退出和运维告警。
- `--preflight` 仅查询 Telegram 机器人及目标聊天；`--send-test` 必须获得用户授权后执行，发送 3 条消息并编辑 1 次，不启动正式推送器。

## 验证状态

- 本地 18 个测试文件、132 个用例通过；包含断网恢复、历史缺口不重复开启、评估期间更新、未知送达有限重试、Telegram 429、持久化在途任务重开及管理员命令/回调鉴权。
- 真实 follow 分页复验仍未通过；最终版本 30 分钟稳定运行已通过（见下）。官方 [track 接口说明](https://github.com/GMGNAI/gmgn-skills/blob/main/skills/gmgn-track/SKILL.md) 声明响应含 `next_page_token`，但 [CLI 实现](https://github.com/GMGNAI/gmgn-skills/blob/main/src/commands/track.ts) 没有提供 follow-wallet 的分页输入选项，不能据此证明具体请求参数有效。
- 本地直连只读预检返回传输错误；随后经用户明确授权，改在 SEA 使用临时凭证完成一次性真实 Telegram 测试。

## 追加证据

- SEA 无网络隔离容器中，真实 HTTP 断连/恢复、HTTP 429 等待/恢复以及持久化数据库重开均通过。
- 分页两次请求各 100 条，第二页重叠 100 条、更旧记录 0、游标未变，仍未通过。
- 新备份包含 3,036 个事件，完整性检查通过；旧镜像只读访问当前数据库通过。
- SEA IPv4 访问 Telegram 根地址 HTTP 302；凭证文件权限 600，数据目录权限 700。
- Telegram 目标与告警目标均确认机器人有管理员身份；共发送 3 条虚构测试消息，编辑 1 次，API 耗时分别为 420/384/438/401ms。临时凭证已删除，正式服务保持 DRY_RUN，不启动 Bot 消费器或正式推送器。
- Photon 链接在未登录浏览器中跳转至钱包连接首页；BullX 跳转至 Telegram 登录页。未连接交易钱包、未点击启动第三方交易机器人，登录后的正确代币定位仍未验证。

## 最终连续运行结果

- 应用提交 `a0c3b2a`，规则版本 `live-2026-09-14.5`，配置版本 `229673f0c68c`。
- 容器从 2026-09-14 10:37:26 UTC 持续运行，最终核对 `restartCount=0`；数据采集仍为 DRY_RUN。
- 验收窗口：2026-09-14 **10:38:36–11:09:31 UTC，共 1,855 秒（30 分 55 秒）**，32 份健康快照；完整日志覆盖 1,858 秒。
- 数据库从 **3,275** 增至 **4,410** 个事件，净增 **1,135**；所有快照完整性检查通过。
- 完整日志：错误 0、429 0、重新启动 0、分页停滞 0、新增缺口 0、水位回退 0；三路水位均推进。本窗口没有触发缺口到期接受。
- 没有出现合格的新推送任务；记录有 7 个对照样本、6 个已过期信号、4 个无效候选。`control` 不代表交易信号。真实消息验收使用单独的虚构测试内容，没有放宽生产筛选条件。

| 来源 | 日志内采集轮数 | 实际平均轮询周期 | 最大轮询周期 |
|---|---:|---:|---:|
| 聪明钱 | 2,049 | 0.906 秒 | 2.571 秒 |
| KOL | 1,120 | 1.660 秒 | 2.247 秒 |
| 关注钱包 | 1,082 | 1.718 秒 | 2.326 秒 |

持续满页会触发提频，以上为实际周期，不是配置的基础间隔。

## 实测耗时

以下指标来自同一进程，统计更新时间为 11:09:27 UTC；累计范围包含正式观测前约一分钟的启动阶段。采集延迟从交易时间算到第一次本地采集入库，包含 GMGN 上游延迟；跨来源已知事件不重复计入。

| 阶段 | 样本数 | P50 | P95 | 最大值 |
|---|---:|---:|---:|---:|
| 聪明钱首次采集延迟 | 606 | 2.901 秒 | 3.621 秒 | 5.959 秒 |
| KOL 首次采集延迟 | 239 | 3.079 秒 | 4.079 秒 | 4.443 秒 |
| 关注钱包首次采集延迟 | 342 | 3.993 秒 | 5.231 秒 | 5.742 秒 |
| 评估排队 | 1,113 | 250ms | 279ms | 630ms |
| 评估执行（含本地快速过滤） | 1,113 | 3ms | 103ms | 5.020 秒 |
| 代币资料读取（含缓存命中） | 378 | 1ms | 108ms | 301ms |
| 钱包画像补查（每批钱包数不同） | 31 | 144ms | 2.934 秒 | 4.838 秒 |

Telegram 一次性测试：首次发送 420ms、升级编辑 384ms、退出提醒 438ms、告警 401ms，每项只有一个真实样本。不能将这些独立阶段的分位数相加当成端到端延迟，也不能据此承诺未来送达时限。

## 仍未闭环的上线事项与处理建议

1. **GMGN 关注分页无进展**：第二页仍为同一批 100 条。需 GMGN 确认有效请求参数/套餐支持；已准备 `GMGN-PAGINATION-ISSUE.md` 脱敏复现说明，未向外部发送。当前轮询稳定不代表历史漏单已补齐。
2. **第三方登录后跳转**：Photon 需要连接钱包，BullX 需要 Telegram 登录。需在用户已有登录会话中核对代币定位；只打开登录页不计为通过。
3. **正式 Bot 常驻交互及真实在途重启**：本轮授权仅限 3 条消息和 1 次编辑，没有开启正式消费器或故意中断真实发送。需另行安排在线命令/按钮与在途请求核对；相关逻辑已通过自动化/持久化数据库测试。

因此已通过本轮持续采集、数值耗时、隔离故障、备份和一次性 Telegram 验收，仍保持 `DRY_RUN=1`，不宣称所有正式上线门禁已通过。

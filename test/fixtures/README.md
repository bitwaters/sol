# 脱敏接口样本

这些样本由公开 GMGN 接口响应脱敏生成，用于验证字段结构、数量单位和计算逻辑。

- 钱包、代币、池子和交易标识替换为一致的 `sample-*` 合成标识。
- 名称、社交账号和图片／网站链接已匿名化；链接使用不可解析的 `example.invalid` 域名。
- 数值、时间戳、标签及字段类型保留，保证回归场景有意义。
- 不包含私人关注钱包列表、API Key、签名私钥或 Telegram 凭证。
- 再次生成真实样本后，应先脱敏再提交；本地原始联调响应放在被忽略的 `data/debug/` 中。
